require 'rails_helper'

RSpec.describe Pick, type: :model do
  it 'is valid with url, image, title, body'

  it 'is invalid without url'

  it 'is invalid without image'

  it 'is invalid without title'

  it 'is invalid without body'
end
