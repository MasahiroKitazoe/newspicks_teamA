$(function() {
  // 「アカウント登録」クリック時、モーダルを表示
  $('#signup-btn').on('click', function(e) {
    e.preventDefault();
    $('#signup').css("display", "block");
  });

  // 背景をクリックするとモーダルを終了
  // 「ログイン」クリック時
});
